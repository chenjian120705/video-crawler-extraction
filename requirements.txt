# FunASR 源码在本仓库 funasr/ 目录，start.bat 已设置 PYTHONPATH，无需 pip install -e .
# 若需 editable 安装，请先创建根目录 README.md 后执行: pip install -e .
#
# ── 推荐安装顺序 ────────────────────────────────────────────────────────────
# 1) 先按下方「PyTorch CUDA」安装 torch / torchaudio（避免 pip 默认装 CPU 版）
# 2) pip install -r requirements.txt
# 3) playwright install chromium   （抖音内置登录需要）
#
# ── PyTorch CUDA（yaowang2035 / Windows + CUDA 13.0 实测）──────────────────
# pip install torch==2.12.0 torchaudio==2.11.0 --index-url https://download.pytorch.org/whl/cu130
# 无 NVIDIA GPU 或仅 CPU 推理时:
# pip install torch torchaudio
# 注意: torch / torchaudio 勿写入下方 pip 列表，以免覆盖已安装的 CUDA 构建。

# ── Gradio Web UI（gradio_ui.py / llm_service.py）──────────────────────────
gradio>=5.50,<6
markdown>=3.5,<4
pillow>=11.0
requests>=2.32,<3
numpy>=2.0

# ── ASR 模型（asr_service.py / model_paths.py / download_models.py）────────
modelscope>=1.36
qwen-asr==0.0.6
openai-whisper>=20250625
# qwen-asr 硬性依赖 transformers==4.57.6；勿写无版本 transformers（会拉到 5.x 引发冲突）
transformers==4.57.6
accelerate>=1.12
# transformers 4.57.6 要求 huggingface-hub>=0.34,<1.0
# 切勿 pin huggingface_hub>=1.0（如 1.16.1），否则 pip 会回退到极旧 tokenizers 并编译失败
huggingface-hub>=0.34.0,<1.0
tokenizers>=0.22,<0.23
safetensors>=0.4.0
tiktoken>=0.13
# qwen-asr 额外依赖（pip 会自动安装，列出便于排查）: nagisa, sox, soynlp, flask, pytz
qwen-omni-utils>=0.0.9

# ── 音频 / 深度学习 ─────────────────────────────────────────────────────────
soundfile>=0.12.1
librosa>=0.11
scipy>=1.4.1

# ── FunASR 核心依赖（setup.py install_requires + funasr/ 源码 import）──────
jamo
PyYAML>=5.1.2
kaldiio>=2.17.0
torch-complex
sentencepiece
jieba
pytorch-wpe
editdistance>=0.5.2
oss2
tqdm
umap-learn
jaconv
hydra-core>=1.3.2
omegaconf>=2.3
tensorboardX

# ── yt-dlp 运行时依赖（源码在 yt-dlp/，ytdlp_bridge.py 无需 pip install yt-dlp）
brotli
certifi
mutagen
pycryptodomex
urllib3>=2.0.2,<3
yt-dlp-ejs==0.8.0
curl-cffi>=0.15,<0.16
cffi
pycparser
charset-normalizer
idna

# ── 抖音内置登录（yt-dlp/douyin_login.py；安装后需: playwright install chromium）
playwright==1.55.0
